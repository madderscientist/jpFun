# 架构
宗旨：框架实现基础机制，具体功能由函数类自己实现。函数之间松耦合。

## 源码解析
首先进行[源码解析](../src/parser/parserContext.ts)，得到函数级别的含义。这一步的主要作用是去糖、固化参数。

基本流程如下：
1. 语法层的解析（`ParserContext.parseGrammar`），即识别 函数调用、标签、大括号，对于不认识的字符，进行语法糖尝试。这一阶段得到的类型在 [grammarType](../src/parser/grammarType.ts) 中定义，目标是得到初步的该层 Node 划分，使得下一步去糖能方便地搜索顶层。
2. 基于第一阶段的 `GrammarNode` 进行具体函数节点的创建，并进行第二轮语法糖解析。

以上两个阶段在同一层进行，更深一层一般出现在函数的参数中，在解析参数的时候自动构建了一棵树。

之前是没有第二阶段的，相当于现在的第一阶段直接从字符串创建具体函数节点。对于有终止符的语法糖，此时后面待处理的还都是字符串，需要在当前层级找到终止符较为麻烦，需要通过“注入语法糖”这种侵入式方法拦截，too trick，也不太优雅。现在将函数的创建建立在 Node 层级而不是字符层级，结构更加清晰好懂。

解析器只负责解析基本语法和调度，函数提供参数定义，解析器根据定义把解析好的内容交给函数，函数自行处理参数。如果遇到基础语法之外的字符，则尝试用语法糖解析，此时解析器调用所有函数的去糖函数。如果没有一个去糖函数能消费，则认为是无用字符。

### 语法糖解析
有三类：
1. 消费未来的字符，如 `note`。在第一阶段完成
2. 消费未来的字符和过去的节点，如 `div`。在第一阶段创建锚点，第二阶段消费过去的节点。
3. 消费未来的字符和未来的节点，如 `voice`。在第一阶段创建锚点，第二阶段消费过去的节点；对于未来的节点，在第二阶段搜索终止符（若无终止符则到末尾），进行子区间的 `GrammarNode` 解析（而不是基于字符级别）。

## 时间信息固化【没做】
对于调性什么的需要时间维度进行固化，这一步会给所有节点分配时间

之后的两个树是两个相对独立的分支。他们之间通过源码树进行链接。【我在想是不是要引入id、用Map进行索引？还是直接引用对象？感觉前者更好维护】
## 音乐语义树
从源码解析树中创建

## 排版树
从源码解析树中创建