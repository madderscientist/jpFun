---
title: 函数速查
description: jpFun 内置函数的参数、默认值、别名与常用写法。
tableOfContents: false
sidebar:
  order: 0
---

import FunctionReference from "../../../../components/FunctionReference.astro";

函数的参数、默认值和用法与当前版本保持同步。`content` 表示音符或花括号包裹的内容，`label` 表示已声明的标签，`length` 可使用 `px` 或 `em`。

<FunctionReference />